const { resolve } = require('path')

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), // Redireciona o padrão que é o 'config/config.js'
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
}